import { editItem } from "@/entities";

// type sss = Record<string, any>;
// 暂时没有扩展项，原本是考虑根据fileTypeId来扩展项，比如窗帘，控制是否折叠
export function outFileDataExtension(fileTypeId: string): editItem[] {
  return []
}
